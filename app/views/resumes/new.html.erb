




<div class="col-md-10 col-centerd">
<div class="panel panel-default">
  <div class="panel-heading"><h3> 投交履历到 <%= @job.title %> </h3></div>
  <div class="panel-body">
    <hr>
    <%= simple_form_for [@job, @resume] do |f| %>
     <%= f.input :content %>
     <%= f.input :attachment %>
     <%= f.submit "送出" %>
    <% end %>
  </div>
</div>
</div>
